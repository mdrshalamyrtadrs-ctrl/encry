# ===========================================
# Environment Variables - NEVER COMMIT REAL VALUES
# Copy this file to .env.local and fill in real values
# ===========================================

# Site URL (your Vercel deployment URL)
SITE_URL=https://your-site.vercel.app

# API Secret Key (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here

# Data Encryption Key - 64 hex chars (generate with: openssl rand -hex 32)
DATA_KEY=your-32-byte-hex-key-here

# Session Secret (generate with: openssl rand -hex 32)
SESSION_SECRET=your-session-secret-here

# Signature Secret for request signing (generate with: openssl rand -hex 32)
SIGNATURE_SECRET=your-signature-secret-here

# Stream Secret for video URL tokens (generate with: openssl rand -hex 32)
STREAM_SECRET=your-stream-secret-here

# Internal Key for dynamic key generation (generate with: openssl rand -hex 32)
INTERNAL_KEY=your-internal-key-here

# Password Salt for password hashing (generate with: openssl rand -hex 16)
PASSWORD_SALT=your-password-salt-here

# Authentication API URL (your backend auth endpoint)
AUTH_API_URL=https://your-auth-api.com/endpoint
